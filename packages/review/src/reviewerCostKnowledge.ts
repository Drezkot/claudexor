import { UsageCostTracker } from "@claudexor/budget";

/** Review attempts share the same observed amount and billing classifier as task attempts. */
export class ReviewerCostKnowledge extends UsageCostTracker {}
