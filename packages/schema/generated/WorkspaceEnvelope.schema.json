{
  "$ref": "#/definitions/WorkspaceEnvelope",
  "definitions": {
    "WorkspaceEnvelope": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Envelope id."
        },
        "task_id": {
          "type": "string",
          "minLength": 1,
          "description": "Task the envelope belongs to."
        },
        "attempt_id": {
          "type": "string",
          "minLength": 1,
          "description": "Attempt the envelope belongs to."
        },
        "repo_root": {
          "type": "string",
          "description": "Absolute path of the source repository root."
        },
        "base_ref": {
          "type": [
            "string",
            "null"
          ],
          "description": "Git base ref; null for directory execution."
        },
        "base_sha": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Resolved base commit SHA; null when not recorded."
        },
        "worktree_path": {
          "type": "string",
          "description": "Absolute actual execution directory, whether Git-backed or plain."
        },
        "branch_name": {
          "type": [
            "string",
            "null"
          ],
          "description": "Clone-local branch; null for directory execution."
        },
        "workspace_kind": {
          "type": "string",
          "enum": [
            "git",
            "directory"
          ]
        },
        "scope_paths": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "home_dir": {
          "type": "string",
          "description": "Scoped HOME directory for the harness process (kept outside the checkout)."
        },
        "harness_config_dirs": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Scoped per-harness config directories keyed by harness id (kept outside the checkout)."
        },
        "policy_profile": {
          "type": "string",
          "default": "workspace_write",
          "description": "Access profile the envelope enforces."
        },
        "dirty_policy": {
          "type": "string",
          "enum": [
            "refuse",
            "snapshot"
          ],
          "description": "How a dirty working tree is handled when creating an envelope: snapshot captures the dirty state as the base SHA without touching the live tree; refuse fails loudly.",
          "default": "refuse"
        },
        "created_at": {
          "type": "string",
          "minLength": 1,
          "description": "When the envelope was created."
        }
      },
      "required": [
        "id",
        "task_id",
        "attempt_id",
        "repo_root",
        "base_ref",
        "worktree_path",
        "branch_name",
        "home_dir",
        "created_at"
      ],
      "additionalProperties": false,
      "description": "Execution directory and scoped HOME/configs. Git checkouts retain their Git identity; directory execution uses a selected copy or the live source without Git initialization."
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
