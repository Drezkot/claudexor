{
  "$ref": "#/definitions/JournaledRunCreatedPayload",
  "definitions": {
    "JournaledRunCreatedPayload": {
      "type": "object",
      "properties": {
        "mode": {
          "type": "string",
          "enum": [
            "ask",
            "plan",
            "agent"
          ],
          "description": "Canonical run mode: ask and plan are read-only, agent writes. Strategy controls are separate schema-owned fields, never modes."
        },
        "prompt_sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "sha256 hex digest of the redacted prompt text."
        },
        "prompt_bytes": {
          "type": "integer",
          "minimum": 0,
          "description": "UTF-8 byte length of the redacted prompt text."
        }
      },
      "required": [
        "mode",
        "prompt_sha256",
        "prompt_bytes"
      ],
      "additionalProperties": true,
      "description": "Journaled `run.created` payload: the prompt text is replaced by its sha256 digest and byte length in the global/project journal partition; the per-run event log keeps the prompt."
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
