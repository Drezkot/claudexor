{
  "$ref": "#/definitions/WorkProduct",
  "definitions": {
    "WorkProduct": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Work product id."
        },
        "kind": {
          "type": "string",
          "enum": [
            "patch",
            "new_repo",
            "report",
            "files"
          ],
          "description": "Kind of work product: Git patch, new repository, report, or complete directory file manifest with retained bytes."
        },
        "source_task_id": {
          "type": "string",
          "minLength": 1,
          "description": "Task the work product came from."
        },
        "producer_attempt_id": {
          "type": "string",
          "minLength": 1,
          "description": "Attempt that produced it, when known."
        },
        "files": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Kind-specific file payload, e.g. artifact name to path (validated loosely here; specialized per kind by callers)."
        },
        "meta": {
          "type": "object",
          "additionalProperties": {},
          "default": {},
          "description": "Kind-specific metadata."
        }
      },
      "required": [
        "id",
        "kind",
        "source_task_id"
      ],
      "additionalProperties": false,
      "description": "The deliverable a run produced, referenced by apply/delivery verbs; files uses files.manifest and meta.manifest_sha256."
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
